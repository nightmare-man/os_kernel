%.o:%.c    #'%'只会选中所有的依赖文件或者目标文件（取决于在冒号坐标还是右边），没有出现在makefile文件里的 .o .c文件即使存在在文件夹
	gcc -c -o $@ $^   #也不会被%选中 通配符 %，会对每一个符号条件的都执行一次命令  而$@则用来代替所有目标文件 $^替代所有依赖文件
objfiles = 1.o 2.o#定义变量
test.bin:${objfiles}
	gcc -o $@ $^
all:test.bin
	@echo "compile done"#加@符号在命令前可以不打印命令本身